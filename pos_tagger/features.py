


def extract_features(sentences, test_sentences=None)
    print "-- Extract features"
    if test_sentences != None:
        sentences = sentences + test_sentences

    print "size of sentences %i"  %len(sentences)